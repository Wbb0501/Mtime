// Type definitions for Hap
// Definitions by: Hap <https://www.quickapp.cn/>
/**
 * 微信支付
 * 接口声明: {"name": "service.wxpay"}
 */
export default class wxpay {
    /**
     * 获取当前可用的微信支付调用方式
     * @returns APP: 微信app调用方式，服务端向微信支付下单时，trade_type需要是APP; MWEB: 微信网页调用方式，服务端向微信支付下单时，trade_type需要是MWEB; none: 微信未安装; 返回值为APP时, 开发者将只能使用app方式, 不能使用网页方式
     */
    static getType(): any

    /**
     * 发起微信支付
     * @param obj
     * @property prepayid 微信支付服务器生成的预支付订单id
     * @property extra 当前支付方式下，需要填入的额外订单信息
     * @property success 成功后的回调函数，App方式下，回调发生在用户支付完成之后，网页方式下，回调发生在订单提交给微信app之后
     * @property fail 失败回调
     * @property complete 执行结束后的回调
     */
    static pay(obj: {
        prepayid: string,
        extra: object,
        success?: Function,
        fail?: Function,
        complete?: Function
    }): void
}
